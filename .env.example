# SuccessFactors API base URL — replace dc with your data center (e.g. api4, api8, apisalesdemo)
SF_BASE_URL=https://api4.successfactors.com/odata/v2

# Basic auth credentials — username is usually in the format: user@COMPANY_ID
SF_USERNAME=admin@COMPANY_ID
SF_PASSWORD=your_password
